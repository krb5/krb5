thisconfigdir=./
BUILDTOP=$(U)$(U)$(U)$(U)
CFLAGS = $(CCOPTS) $(DEFS)

##DOSBUILDTOP = ..\..\..\..
##DOSLIBNAME=..\..\krb5.lib

.c.o:
	$(CC) $(CFLAGS) -c $(srcdir)/$*.c
@SHARED_RULE@

SRCS= $(srcdir)/dbkeytab.c 

OBJS = dbkeytab.$(OBJEXT)	

all-unix:: shared $(OBJS)
all-mac:: $(OBJS)
all-windows:: $(OBJS)

shared:
	mkdir shared

clean-unix::
	$(RM) shared/*

clean-mac::
clean-windows::
