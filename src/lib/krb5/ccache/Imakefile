#define IHaveSubdirs
#define PassCDebugFlags

        SUBDIRS = file

MakeSubdirs($(SUBDIRS))

NormalLibraryObjectRule()

OBJS=	default.o \
	resolve.o \
	dir.o

SRCS=	default.c \
	resolve.c \
	dir.c

NormalLibraryTarget(cred,$(OBJS))
