CFLAGS = $(CCOPTS) $(DEFS)

##DOSBUILDTOP = ..\..\..
##DOSLIBNAME=..\crypto.lib
##DOS!include $(BUILDTOP)\config\windows.in

LIBOBJS = @LIBOBJS@

OBJS=	rnd_confoun.$(OBJEXT) localaddr.$(OBJEXT) ustime.$(OBJEXT) $(LIBOBJS)

SRCS=	rnd_confoun.c

all:: all-$(WHAT)
all-unix:: $(OBJS)
all-mac:: $(OBJS)
all-windows: rnd_confoun.obj

localaddr.c: $(C)$(U)$(U)krb5$(S)os$(S)localaddr.c
	-$(LN) $(srcdir)$(U)$(U)krb5$(S)os$(S)localaddr.c $@

ustime.c: $(C)$(U)$(U)krb5$(S)os$(S)ustime.c
	-$(LN) $(srcdir)$(U)$(U)krb5$(S)os$(S)ustime.c $@


memmove.c: $(C)$(U)$(U)krb5$(S)posix$(S)memmove.c
	-$(LN) $(srcdir)$(U)$(U)krb5$(S)posix$(S)memmove.c $@

clean::
	$(RM) ustime.c localaddr.c memmove.c
