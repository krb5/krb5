CFLAGS = $(CCOPTS) $(DEFS) $(LOCALINCLUDES)
LDFLAGS = -g

OBJS=resolve.o
SRCS=$(srcdir)/resolve.c

all:: resolve


resolve: $(OBJS)
	$(LD) $(CFLAGS) -o resolve $(OBJS) $(LIBS)

check:: resolve
	./resolve

install::

clean::
	$(RM) resolve

